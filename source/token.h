#ifndef TOKEN_H
#define TOKEN_H

// Replace the placeholder below with your actual GitLab private token if you
// do not want to supply one at runtime via the GITLAB_PRIVATE_TOKEN environment
// variable. Keep the quotes around the token string.
#define GITLAB_PRIVATE_TOKEN "YOUR_ACTUAL_GITLAB_TOKEN_HERE"

#endif // TOKEN_H
